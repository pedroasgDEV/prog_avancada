INÍCIO
    LER o número de casos de teste

    PARA cada caso de teste:
        LER as taxas de pedágio para cada hora (pedágios[0] até pedágios[23])

        LER os registros de entrada:
            PARA cada linha de registro:
                Criar um objeto Registro e adicionar à lista de registros
        
        ORDENAR registros pela ordem de licença e depois data (utilizando o operador <)
        
        INICIALIZAR uma lista de contas vazia
        
        PARA i de 0 até tamanho de registros - 1:
            SE a licença do registros[i] for diferente da licença do registros[i+1]:
                incrementar i e continuar
            SE o tipo de registros[i] não for "enter" ou tipo de registros[i+1] não for "exit":
                incrementar i e continuar

            BUSCAR uma conta com a licença do registro[i]
            
            SE não encontrar:
                CRIAR nova conta com a licença do registros[i] e SOMA 2 a fatura da conta 
                ADICIONAR a conta à lista de resultado
            
            SOMAR taxa da viagem à fatura da conta     
            // taxa da viagem = ((distancia pecorrida) * (taxa da hora de entrada) ) / 100 + 1 

            incrementar i por 2
        
        ORDENAR a lista de contas pela licença (utilizando o operador <)

        PARA cada conta na lista de contas:
            imprimir conta

FIM
